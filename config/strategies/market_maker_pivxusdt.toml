# ═══════════════════════════════════════════════════════════════════════════
# MarketMaker Strategy Configuration — PIVXUSDT
# ═══════════════════════════════════════════════════════════════════════════
#
# PIVXUSDT characteristics:
#   Price: ~$0.077
#   Tick size: 0.0001
#   1 tick = ~130 bps spread
#   Step size: 1.0
#
# Strategy: Buy passive at bid, sell passive at ask.
# Net edge per round-trip: ~110 bps (130 spread - 20 maker fees)

# --- Spread & pricing ---
min_spread_bps = 60.0
round_trip_cost_bps = 20.0
min_edge_bps = 15.0

# --- Position management ---
max_hold_millis = 120000
stop_loss_bps = 220.0
entry_cooldown_millis = 15000

# --- Volatility filters ---
max_vol_bps = 1000.0
panic_vol_bps = 2000.0

# --- Trade window ---
trade_window_millis = 60000
min_trades_in_window = 2

# --- Order book imbalance ---
imbalance_skew = 0.0
max_imbalance = 1.0

# --- Trade direction filter ---
require_seller_initiated = true
min_sell_flow_fraction = 0.0

# Price position filtering
price_position_window = 20
max_price_position = 0.3

# --- Sizing ---
cash_fraction = 0.06
